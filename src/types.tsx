type Person = {
    name: string,
    job: string,
    placeOfWork: string,
    description: string,
    utility: string,
    latestMeetingDate: string,
    isUser: boolean,
    closeness: number,
    people: Person[]
}