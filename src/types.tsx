type Person = {
    name: string,
    initials: string,
    job: string,
    placeOfWork: string,
    description: string,
    utility: string,
    latestMeetingDate: string,
    isUser: boolean,
    closeness: number,
    connections: Person[]
}
